[package]
name = "livetap-shared"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
uniffi = { version = "0.29", optional = true }

[features]
uniffi = ["dep:uniffi"]
